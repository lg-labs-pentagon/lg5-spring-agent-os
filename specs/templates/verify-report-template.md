---
kind: template
name: verify-report-template
version: 0.1.0
description: Verification report cross-checking every PRD acceptance criterion against test evidence after all TASKs are done. Produced by /sdd-verify. Blocks spec closure when AC are red unless overridden by a tech-debt ADR.
---

# Verify Report — `<feature-name>`

> **Use this template via `/sdd-verify`.** Replace every `<placeholder>`.
> Produced **after** every TASK in [`tasks.md`](tasks.md) is `Status: done`.
> The report is the **gate** that turns a spec from "implemented" into
> "verified". A spec with a red verify-report cannot be closed unless a
> dedicated `tech-debt` ADR overrides the gap.

## 0. Run metadata

| Field | Value |
|-------|-------|
| Generated by | `sdd-verifier` |
| Run timestamp (UTC) | `<YYYY-MM-DDTHH:MM:SSZ>` |
| Git SHA | `<commit-sha>` |
| Branch | `<branch>` |
| Build command | `make all-build` |
| Acceptance command | `make run-acceptance-test` |
| Build result | ✅ pass / ❌ fail |
| Acceptance result | ✅ pass / ⚠ flaky / ❌ fail |

## 1. AC ↔ evidence matrix

> One row per REQ × AC pair. Evidence is a concrete artifact (test
> method, feature step, log line, report file). Status must be one of:
>
> - ✅ **pass** — AC is asserted by an automated test that passed.
> - ⚠ **flaky** — AC has coverage but the test was unstable in this run.
> - ❌ **fail** — AC has coverage but the test failed.
> - ⚪ **uncovered** — no test references this AC.
> - 🟡 **manual** — AC verified by a human procedure (see §4).

| REQ | AC summary | Covered by | Evidence | Status |
|-----|------------|------------|----------|:-----:|
| REQ-001 | `<one-line AC>` | `<TestClass.method>` | `target/surefire-reports/<file>` | ✅ |
| REQ-001 | `<one-line AC>` | `<feature>.feature:<line>` | `allure-results/<uuid>.json` | ✅ |
| REQ-002 | `<one-line AC>` | — | — | ⚪ |
| REQ-003 | `<one-line AC>` | `<TestClass.method>` | `target/surefire-reports/<file>` | ❌ |

## 2. Coverage summary

| Bucket | Count | % |
|--------|------:|--:|
| ✅ pass | `<n>` | `<n>%` |
| ⚠ flaky | `<n>` | `<n>%` |
| ❌ fail | `<n>` | `<n>%` |
| ⚪ uncovered | `<n>` | `<n>%` |
| 🟡 manual | `<n>` | `<n>%` |
| **Total AC** | **`<n>`** | **100%** |

## 3. TASK ↔ REQ traceability

> Confirms that every REQ from the PRD made it into a completed TASK.

| REQ | Implemented by TASK(s) | Test added by TASK(s) | Status |
|-----|------------------------|------------------------|:-----:|
| REQ-001 | TASK-002, TASK-005 | TASK-006 | ✅ |
| REQ-002 | — | — | ⚪ MISSING |

## 4. Manual verifications (if any)

> AC that cannot be automated — operations runbook, dashboard checks,
> 3rd-party-confirmation. Each row needs an owner and a date.

| REQ | AC | Procedure | Verified by | Date | Status |
|-----|----|-----------|-------------|------|:-----:|
| REQ-NNN | `<AC>` | `<runbook link>` | `<name/role>` | `<date>` | 🟡 |

## 5. Constitutional check

> Cross-check that nothing implemented contradicts the constitution. A
> single ❌ here blocks the gate regardless of AC coverage.

| RULE | Touched by feature? | Verdict | Evidence |
|------|:-------------------:|:------:|----------|
| RULE-001 (stack) | yes | ✅ | `pom.xml` parent + JDK version |
| RULE-006 (vnd.api.v1+json) | yes | ✅ | `<Controller>.java` `produces=` |
| RULE-008 (outbox) | yes | ✅ | `<Entity>OutboxJpaEntity` `@Version` + `OutboxStatus` |
| RULE-010 (NO-OP listener) | yes | ⚠ | manual review — see §4 row 2 |
| RULE-NNN | no | n/a | — |

## 6. Gaps and overrides

> Any non-✅ entry above must appear here with a remediation decision.

| Gap | Severity | Decision | Owner | Tracking |
|-----|----------|----------|-------|----------|
| REQ-002 uncovered | blocker | open follow-up TASK | `<role>` | `<issue/PR>` |
| RULE-010 ⚠ | advisory | accept (manual review passed) | `<role>` | — |
| REQ-003 ❌ | blocker | new ADR `tech-debt-NNN`: time-boxed override until `<event>` | `<role>` | `adr/ADR-NNN-…md` |

## 7. Gate decision

> Pick **one**. Verifier writes the decision; human approves it.

- [ ] ✅ **VERIFIED** — all AC ✅ (or ⚠ accepted), all RULE ✅.
- [ ] 🟡 **VERIFIED WITH OVERRIDE** — some AC/RULE non-✅ but covered by
      a tech-debt ADR with explicit time-box.
- [ ] ❌ **NOT VERIFIED** — gaps remain that are neither resolved nor
      formally overridden. **Spec cannot be closed.**

## 8. Suggested next step

- If ✅ or 🟡: human marks spec as `verified`; bundle moves to next feature.
- If ❌:
  - Re-open the most appropriate phase (`/sdd-tasks`, `/sdd-implement`,
    or `/sdd-design`).
  - For accepted overrides, ensure the new ADR is committed under
    `docs/specs/<NNN-slug>/adr/`.

## Definition of Done (Verify report)

- [ ] §0 run metadata fully populated (timestamps, SHA, results).
- [ ] §1 matrix has one row per REQ × AC pair from the PRD (no omissions).
- [ ] Every non-✅ row in §1, §3, or §5 appears in §6 with a decision.
- [ ] §7 gate decision is explicitly ticked.
- [ ] If gate is ❌: a follow-up plan is named (re-run phase or ADR).
- [ ] If gate is 🟡: at least one `tech-debt` ADR is linked in §6.
- [ ] Report committed at `docs/specs/<NNN-slug>/verify-report.md`.
